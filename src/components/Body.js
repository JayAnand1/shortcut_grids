import React, { useState } from "react";
import { Grid } from "@material-ui/core";

const Body = () => {
  return (
    <Grid
      container
      spacing={2}
      direction="row"
      alignItems="stretch"
      justify="center"
    ></Grid>
  );
};

export default Body;
